<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace指向mapper接口 -->
<mapper namespace="com.wt.restaurant.dao.dishorder.IDishOrderMapper">
	
	<select id="listDishOrder" resultType="DishOrder">
		SELECT
		id,
		orderNumber,
	    reserveType,
	    createTime,
	    totalCount,
	    totalAmount,
	    discountAmount,
	    totalPayAmount,
	    orderStatus
		FROM
		dish_order
		ORDER BY createTime desc
		LIMIT
		${(currentPageNo-1)*pageSize},${pageSize}
	</select>

	<select id="listDishOrderByCustomerId" resultType="DishOrder">
		SELECT
		id,
		number,
		image
		FROM table
		WHERE
		id=#{id}
	</select>
	
	<select id="countDishOrder" resultType="Integer">
		SELECT COUNT(*) FROM
		dish_order
	</select>

</mapper>